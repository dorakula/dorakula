# DORAKULA v3.1.0 — Python dependencies
# Directly imported third-party packages (audit: Session 6, 2026-06-24)

# Web framework + WSGI
flask>=2.3.0
werkzeug>=2.31.0

# MCP (Model Context Protocol) server
mcp>=0.2.0
fastmcp>=0.4.0
starlette>=0.37.0
uvicorn>=0.30.0

# HTTP clients
requests>=2.28.0
urllib3>=2.0.0
aiohttp>=3.8.0

# WebSocket clients
websockets>=12.0
websocket-client>=1.7.0

# DNS (subdomain takeover checks)
dnspython>=2.6.0

# System monitoring
psutil>=5.9.0

# Data validation (transitive via mcp, pinned for stability)
pydantic>=2.0.0
